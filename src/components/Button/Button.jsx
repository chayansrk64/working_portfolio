 
const Button = ({button_text}) => {
    return (
        <div>
            <button className="border-2 border-white px-4 py-2">{button_text}</button>
        </div>
    );
};

export default Button;